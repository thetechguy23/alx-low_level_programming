#include <stdio.h>

/**
 * main - Print alphabet in lowercase
 * followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
